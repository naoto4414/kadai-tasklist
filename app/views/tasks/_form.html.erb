<%= form_for(task) do |f| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <ul>
        <% task.errors.full_task.each do |task| %>
          <li><%= task %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :task, 'タスク' %>
  <%= f.text_field :task %>

  <%= f.label :status, 'メッセージ' %>
  <%= f.text_field :status %>

  <%= f.submit '投稿' %>
<% end %>